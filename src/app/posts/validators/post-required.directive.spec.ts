import { PostRequiredDirective } from './post-required.directive';

describe('PostRequiredDirective', () => {
  it('should create an instance', () => {
    const directive = new PostRequiredDirective();
    expect(directive).toBeTruthy();
  });
});
